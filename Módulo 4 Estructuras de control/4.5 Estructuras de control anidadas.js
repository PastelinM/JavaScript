/**
 * @estructuralControlAnidada
 *Una estructura de control anidada en la programación se refiere a la inclusión de una o más estructuras de control dentro de otra.
 */

/**
 * @ejemplo
 */

if (condicion1) {
    // Código cuando la condición 1 es verdadera
    if (condicion2) {
        // Código cuando ambas condiciones son verdaderas
    } else {
        // Código cuando la condición 1 es verdadera y la condición 2 es falsa
    }
} else {
    // Código cuando la condición 1 es falsa
}
